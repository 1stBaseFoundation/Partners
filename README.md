# Partners

Custom scripts for developers/partners/exchanges to manage wallets and transactions.

1. PHP Script to generate new wallets on a local node, with optional arguments to return pubKey, privKey and wordphrase.
2. PHP Script to generate transactions on a local node, fetches UTXOs and signs transactions before submitting to the mempool for mining.
